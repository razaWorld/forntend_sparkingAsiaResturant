

### ✅ Core Redux & Navigation Libraries:

```bash
npm install @reduxjs/toolkit react-redux redux-persist
npm install @react-navigation/native @react-navigation/native-stack @react-navigation/stack
npm install @react-navigation/bottom-tabs @react-navigation/material-bottom-tabs
npm install react-native-screens react-native-safe-area-context
```

---

### ✅ Async Storage & i18n:

```bash
npm install @react-native-async-storage/async-storage
npm install i18next react-i18next
```

---

### ✅ UI Libraries:

```bash
npm install react-native-modal react-native-keyboard-aware-scroll-view
npm install react-native-linear-gradient
npm install react-native-svg react-native-svg-transformer
npm install react-native-svg-circle
```

---

### ✅ Image & Picker Tools:

```bash
npm install react-native-image-crop-picker
npm install react-native-image-picker
npm install @react-native-community/datetimepicker
```

---

### ✅ Google/Firebase & Location Services:

```bash
npm install @react-native-google-signin/google-signin
npm install react-native-geocoding
npm install react-native-get-location
npm install @react-native-community/geolocation
npm install react-native-geolocation-service
npm install react-native-google-places-autocomplete
npm install @react-native-firebase/app @react-native-firebase/database @react-native-firebase/firestore
```

---

### ✅ Maps:

```bash
npm install react-native-maps react-native-maps-directions
```

---

### ✅ Masonry & Gallery:

```bash
npm install react-native-masonry-list
npm install @react-native-community/image-gallery
```

---

### ✅ Carousel & Animation:

```bash
npm install react-native-reanimated-carousel react-native-reanimated
```

---

### ✅ Fonts & Assets:

```bash
npm i expo-font
npx react-native-asset
```

---

### ✅ Utilities:

```bash
npm install lodash.debounce
```

---

### ✅ Testing:

```bash
npm install --save-dev jest @testing-library/react-native @testing-library/jest-native react-test-renderer
```

---
npm install --save-dev typescript @types/react @types/react-native


Let me know if you want a script to install all of these in one go.
